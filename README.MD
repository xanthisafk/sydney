# 🦘 Sydney

---

A lightweight, edge-based control plane for S3.

Uses bucket as dumb storage and Sydney as source of truth.

Doesn't buffer binaries on edge, streams them directly from S3.

Currently designed for images and videos and single user.
